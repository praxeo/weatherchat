name = "weather-chat"
main = "index.js"
compatibility_date = "2026-05-28"
compatibility_flags = ["nodejs_compat"]

[observability]
enabled = false

[vars]
NWS_USER_AGENT = "WeatherChatBot/1.0 (contact@example.com)"
MODEL = "qwen/qwen3.8-27b"
DEFAULT_LAT = "33.21"
DEFAULT_LON = "-86.65"
DEFAULT_OFFICE = "BMX"
DEFAULT_LOCATION_NAME = "Shelby County, Alabama"
# Optional inference tuning (all have sensible defaults if unset):
#   REASONING_EFFORT — Qwen3.8 27B reasoning depth: "none" (disabled), "low"
#     (default here, fastest), "medium", or "high" (most thorough — selects
#     the model's native xhigh mode). Groq's own model default is "none".
#   MAX_TOKENS — max output tokens per turn (default 8192; model max 16384).
#   SUMMARY_MODEL — model used for the at-a-glance home-screen forecast line
#     (/api/summary). Defaults to MODEL. Set a cheaper/faster model here to
#     decouple the one-liner from the main chat model.

# GROQ_API_KEY is a required secret (inference provider). Set it with:
#   wrangler secret put GROQ_API_KEY

# AIRNOW_API_KEY is a secret. Set it with:
#   wrangler secret put AIRNOW_API_KEY
# (without it, get_air_quality returns a helpful "not configured" message.)

# ELEVENLABS_API_KEY is a secret (voice input: speech-to-text). Set it with:
#   wrangler secret put ELEVENLABS_API_KEY
# (without it, /api/transcribe returns a "not configured" error and the rest
# of the app works normally.)

# MISTRAL_API_KEY is a secret (voice output: Mistral Voxtral text-to-speech).
# Set it with:
#   wrangler secret put MISTRAL_API_KEY
# (without it, /api/tts returns a "not configured" error and the rest of the
# app works normally.)
# Optional: MISTRAL_TTS_VOICE_ID selects a preset or cloned voice (see
# Mistral's /v1/audio/voices). Left unset, Mistral uses its own default voice.
